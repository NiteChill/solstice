// import { useAuth } from '../../auth/hooks/use-auth';
import { ProfileForm } from './profile-form';

export const ProfileSection = () => {
  // const { user } = useAuth();
  return (
    <>
      <ProfileForm />
      {/* Display role, creation date and last update date */}
      {/* Delete account button */}
    </>
  );
};
